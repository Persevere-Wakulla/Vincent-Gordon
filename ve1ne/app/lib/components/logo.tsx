import clsx from 'clsx'
import { cinzel } from '@/lib/fonts/fonts';
const Logo = () => {
    return (
        <h3 className={clsx(cinzel.className,"bg-fenix bg-no-repeat bg-center  text-center text-6xl font-medium p-10 rounded-lg  leading-9 tracking-tight text-indigo-700")}>
        Ve1ne
    </h3>
    );
}

export default Logo;